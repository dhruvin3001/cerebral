---
description: Save session learnings to cerebral permanent memory
---

Review this entire session and call the `save_session_learnings` tool with a thorough summary including:

- Every correction the user made ("no not like that", "don't do X")
- Every non-obvious discovery about the codebase
- Every architectural decision made and the reasoning behind it
- Any API quirks, workarounds, or undocumented behavior found
- Any user preferences revealed through feedback or confirmation

Be thorough — this is the permanent record that survives session end and will be loaded in future sessions.
