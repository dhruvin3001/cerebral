[project]
name = "cerebral"
version = "0.1.0"
description = "Permanent memory MCP server for AI coding agents"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mem0ai",
    "mcp[cli]",
    "python-dotenv",
    "qdrant-client",
]
